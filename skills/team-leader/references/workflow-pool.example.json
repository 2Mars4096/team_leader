{
  "reviewer": {
    "role": "visual comparison reviewer",
    "instructions": "Compare only evidence visible in the supplied references. Identify the asset version and reference paths. Report missing views as uncertain.",
    "sandbox": "read-only",
    "max_parallel": 2,
    "max_session_steps": 12,
    "max_run_seconds": 900
  },
  "editor": {
    "role": "scoped model editor",
    "instructions": "Revise only the current part, preserve adjacent geometry, and report before/after evidence and affected views.",
    "sandbox": "workspace-write",
    "max_parallel": 1,
    "max_session_steps": 8,
    "max_run_seconds": 900
  }
}
